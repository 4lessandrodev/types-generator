import from '@modules/{{dashCase name}}/application/use-cases/create-{{dashCase name}}.use-case.ts';
import I{{pascalCase name}} from '@modules/{{dashCase name}}/domain/repo/{{dashCase name}}.repo.ts';

describe('create-{{dashCase name}}.use-case', ()=> {

    let repo: I{{pascalCase name}}Repo;

    beforeEach(()=>{
        repo = {
            save: jest.fn(),
            findMany: jest.fn()
        }
    });

    it('should be defined', ()=> {
        const useCase = new Create{{pascalCase name}}UseCase(repo);
        expect(useCase).toBeDefined();
    });
});
