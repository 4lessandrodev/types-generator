import {{pascalCase name}}Mapper from '@modules/{{dashCase name}}/infra/mappers/{{dashCase name}}.mapper';

describe('{{dashCase name}}.mapper', () => {
    it('should be defined', () => {
        const mapper = new {{pascalCase name}}Mapper();
        expect(mapper.toDomain).toBeDefined();
        expect(mapper.toPersistence).toBeDefined();
    });
});
